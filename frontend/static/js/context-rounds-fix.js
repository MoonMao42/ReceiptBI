// No-op shim to avoid missing asset error.
// The app already handles contextRounds via settings and localStorage.
// This file intentionally left minimal.

(() => {
  // Optional: ensure window.app exists before access elsewhere
  // Nothing to do here.
})();

