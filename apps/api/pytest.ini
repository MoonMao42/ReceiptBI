[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: tests that require external services such as PostgreSQL, MySQL or mock LLM gateways
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
